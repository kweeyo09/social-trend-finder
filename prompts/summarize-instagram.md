# Instagram Summary

Use the `instagram.hashtags` array (already ranked, highest `peak_velocity` first).
Show up to 5.

For each hashtag:
- Display `#<hashtag>` with its peak velocity and recent post count.
- Velocity = engagement per hour, recency-weighted. It reflects what's
  **accelerating now**, not raw popularity — treat a high velocity as "rising".

Then write a 2-sentence **Take**:
1. What the pattern signals about audience behaviour right now.
2. One specific content action the team should take this week.
