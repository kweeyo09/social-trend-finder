# Dependencies for the SERVER-SIDE feed generator (generate_feed.py), run by
# the GitHub Action only. The Claude skill side (prepare_digest.py, deliver.py)
# uses ONLY the Python standard library — nothing to install to run a digest.

scrapling               # web-fetch backend for base_fetcher.safe_get (ScrapeCreators + Reddit)
tenacity==8.3.0         # retry/backoff on rate limits
python-dotenv==1.0.1    # load .env when testing the fetcher locally
yt-dlp                  # keyless YouTube search + metadata (youtube.py)
